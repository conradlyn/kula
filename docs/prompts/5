// Model: Claude 4.6 Opus (Thinking)

1. add unit tests and make a check.sh that run linter and unit tests
2. The binary is missing proper usage message when --help flag is provided
3. when I use time window selection on a graph it should pause the stream, because otherwise time window resets instantly
4. make "Kula-Szpiegula" in the header clickable and redirect to self (refresh the page)
5. do not display values ​​after the decimal point in the pps and disk i/o graphs tooltip
6. if there is a gap between measurements, i.e. 1 minute, then the graph should display empty space for that minute by default, 
joining metrics should be optional feature to be enabled in the config.yaml
7. if I'm hovering over a graph the stream should be paused, and resumed when I move the mouse away from the graph
8. add focus mode which lets you select graphs to display, and hide the rest
9. add systemd service, openrc and runit files for kula in ./addons/init/
10. find todo comments in the addons directory and also implement them
11. either do not display alert icon if no alerts are present, or make it grayed out
