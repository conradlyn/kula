I'm gonna need to test it in a longer run, but as for now it looks promising.

There's however another problem I've noticed. When I use window 5m, 15m, 30m I see data in 1-second sampling. When I use 1, 3 and 6h I see 1-minute sampling. When I use 24h, 3d, 7d presets I see 5-minute sampling. It looks to me like these presets has been glued to the tiers. 

Unfortunately, this means that even though we have several hours or more of second-level data in Tier 1, we can't use it to detect anything that might simply escape our attention in a larger sampling window. Furthermore, even if I'm in, say, a 3-hour window, even after zooming in, the sampling is still 1 minute long, even though there are clearly 1second-level samples available for that time period.

Additionally I would like to see the sampling / tier used next to time-info in the web UI. 

While we're at UI I would like to change few things:
- add green color to the ✓ time sync status, and red to x when it's not synced
- add a link to the repo https://github.com/c0m4r/kula in the footer

Also add new alerts for when CPU, RAM and SWAP usage is higher than 95%